# Getting Started

## Install express

``` bash
    $ npm init
        ...
        entry point: (index.js) app.js
        ...
    $ npm install express --save
```

## Install Vue

``` bash
    $ npm install -g vue-cli
    $ vue init webpack client
    $ cd client
    $ npm install
```
